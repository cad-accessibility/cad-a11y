# Core web server
Flask>=2.3.0
flask-cors>=6.0.5

# Numeric + image stack
numpy>=1.21.0
Pillow>=9.0.0
matplotlib>=3.11.0
scikit-image

# CAD / geometry stack
trimesh>=3.20.0
shapely>=2.0.0
cascadio
networkx
meshlib
polyscope
svgwrite

# Braille / hardware integrations
hidapi>=0.15.0
pyserial>=3.5
bleak>=0.22.0
godice

# Utility / testing helpers
requests>=2.28.0

# NOTE:
# pythonocc-core is required by the renderer but is typically installed via conda
# (see environment.yml) rather than pip.