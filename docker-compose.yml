services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "8635:6969"
    volumes:
      - uploads:/app/uploads
      - ./accessible-3d-viewer.html:/app/accessible-3d-viewer.html:ro

volumes:
  uploads:
